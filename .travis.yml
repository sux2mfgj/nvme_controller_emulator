language: C

git:
    submodule: false

before_install:
    - sed -i 's/git@github.com:/https:\/\/github.com\//' .gitmodules
    - git submodule update --init --recursive

addons:
    apt:
        packages:
            - build_essential

script:
    - make bhyve_build
